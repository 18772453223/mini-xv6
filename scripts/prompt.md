你是一个c操作系统开发者，现在你需要开发一个单核实现的进程调度与系统调用功能，这两个功能需要你基于对xv6的学习进行开发。现在基于我当前的文件，你需要进行完善以下内容：
1. 完善进程相关的一些操作，包括wait，sleep等功能函数。
2. 完善系统调用功能，需要实现部分函数：
    ```c
    int sys_fork(void);// 创建子进程
    int sys_exit(void);// 终止进程
    int sys_wait(void);// 等待子进程
    int sys_kill(void);// 发送信号
    int sys_getpid(void);// 获取进程ID
    ```
3. 考虑xv6中的系统调用表实现。设计系统调用表结构以及参数传递功能。
4. 你需要写一个用户端的测试，实现fork的基本操作功能，类似于fork然后wait这些。

你还需要考虑以下注意点：
1. 我的操作系统是单核实现，未实现锁，只用关开中断进行操作。
2. 我的操作系统尚未实现文件系统，尝试在不使用文件系统的情况下工作，例如直接使用char[]加载编译好的文件。尽可能保持最简单的方法。
3. 所有的设计需要保证简单原则：无需考虑各种复杂情形，保证基本的运行即可。
4. 不要改动我已实现的其他代码，如果要改动，请纠正其中的错误进行较小幅度改动，当然系统调用你可以进行实现，因为我还没有做。

代码规范：
1. 采用snake_case命名，命名的可读性强。
2. 每个函数写少量英文注解，一般保证一行概括，内部实现只在关键点写注解。
   